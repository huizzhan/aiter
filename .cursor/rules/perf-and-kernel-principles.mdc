---
description: 性能测量、trace 分析与 kernel 改动的基本原则常识
alwaysApply: true
---

# 性能 / Trace / Kernel 工作的基本原则

在涉及性能测量、trace/ISA 分析或修改 kernel 时，始终遵守以下原则。

## 1. 测性能 / 出 trace 前必须确认目标 GPU 利用率和显存占用都为 0（硬性前置门禁）
- 跑 benchmark、profiling、生成 trace 前，**必须先实际执行一次目标 GPU 检查命令并把输出贴出来**，确认 **目标卡 GPU 利用率 ≈ 0% 且显存占用 ≈ 0**（无其它训练/推理/编译/残留进程占用）。这是**强制前置门禁**：未执行检查、或检查未通过，**一律不准开跑**（包括 pytest 性能用例、`rocprofv3` / trace 采集脚本、任何 bench 脚本）。不要凭"应该是空的"假设跳过。
- **只检查实际要用的那张卡**：先确定 `HIP_VISIBLE_DEVICES` / `CUDA_VISIBLE_DEVICES` 指定的物理卡号，只对该卡判定（其它卡被占不影响）。
- 推荐的可直接照搬的检查命令（以 AMD、目标卡 `GPU=${HIP_VISIBLE_DEVICES:-7}` 为例）：
  - 利用率：`rocm-smi -d ${GPU} --showuse` → 期望 `GPU use (%): 0`
  - 显存：`rocm-smi -d ${GPU} --showmemuse` → 期望已用 ≈ 0；或 `rocm-smi --showmeminfo vram -d ${GPU}` 看 `Used Memory` 接近 0。
  - 进程：`rocm-smi --showpids` / `rocm-smi -d ${GPU} --showpidgpus` → 期望该卡无残留 PID。
  - NVIDIA：`nvidia-smi --query-gpu=index,utilization.gpu,memory.used --format=csv -i ${GPU}` → util≈0、memory.used 仅剩驱动基线。
- **判定阈值**：利用率 0%（容忍偶发 1–2% 抖动）、显存已用接近 0（仅驱动/固件基线，无 GB 级框架占用）、无他人/残留 PID。任一不满足即视为**未通过**。
- **未通过时的处理（按优先级）**：① 换一张空闲卡（改 `HIP_VISIBLE_DEVICES` 后重新检查）；② 若占用是本会话上一次跑的**残留进程**，确认安全后清理（kill 残留 PID）并复核显存已释放；③ 是他人进程则等其释放，不要抢占。**在问题解决前不得开跑。**
- **跑完 / 中断后必做收尾**：再次执行上面的检查命令，确认本次进程已退出、显存已释放，避免残留占用影响下一次测量（尤其 pytest/rocprofv3 异常中断后易残留）。
- 在利用率非 0 或显存被占的 GPU 上测出的耗时 / trace 结果**不可信**，不得据此下任何性能结论。
- **小幅差异（±几 % / 几十 us）尤其要警惕卡噪声**：实测案例中，同一份代码在被占卡上测得 510–517us（假性 −6% 回归），换到确认空闲的卡复测才是真实的 ~478us。出现意外的"回归/提升"时，先换一张确认空闲的卡复测，再下结论；A/B 对比务必在**同一张确认空闲的卡**上先后测（消除跨卡差异），并多轮取 median。

## 2. 改 kernel 前先确认能编译 + 运行验证
- 动 kernel 源码前，确认本地能编译并运行（GPU、运行时、框架就绪）。
- 若执行后端不可用（如 `Execution backend unavailable`）或无法验证，**不要盲目改 kernel**——改 LDS 布局 / 读写地址极易引入静默数值错误。先把改法写成草稿，等环境恢复再套用。

## 3. trace/ISA 必须与当前源码版本对齐
- 用任何 trace、ISA dump、hotspot 报告前，先确认它与当前磁盘源码是同一版本（行号、`.loc` 标记、指令形态对得上）。
- 版本不一致时，基于旧产物改新源码会南辕北辙——先用当前源码重新出 trace。

## 4. 优化前先建立 baseline，每步回归验证
- 改动前先记录 baseline：耗时 + 数值正确性（如 `torch.allclose`）。
- 每改一处都重新 bench + 校验正确性，再进行下一步；不要一次堆多个未验证改动。

## 5. 性能结论以实测为准，不要凭推理
- 不要凭代码推理直接下"更快/更慢"的结论；以实测 trace / 计时数据为准。
- 理论分析（如"省了 LDS 往返"）可能与实测瓶颈（如 VMEM-wait、碎 buffer_store）相反——务必用数据印证。

## 6. 主动沉淀有价值的经验到 rules
- 当对话中出现**值得沉淀**的经验时（踩坑教训、反复犯的错、重要约束/前提、非显然的项目惯例），主动询问用户是否要把它加入 `.cursor/rules/`。
- 只在确实值得沉淀时才问，不要为琐碎细节频繁打断；询问时附上拟写入的简洁条目，用户同意后再写入。

## 7. 分析源码前重新读取当前文件，不要依赖记忆
- 分析或修改源码/trace 前，先重新读取当前文件，确认其 revision/版本与内容。
- 不要依赖同一会话中较早读到的内容——文件可能已被修改（如 kernel revision 已更新、某些 LDS 缓冲/代码段已被删除），基于过期内容的判断会出错。

## 8. 运行覆盖性操作前先确认不会误删既有产物
- 运行任何带 `rm -rf` / 会覆盖输出目录的脚本（如 trace 采集脚本）前，先检查目标输出目录是否已有需要保留的数据。
- 必要时先备份、或改用新的输出路径，避免覆盖既有 trace / 结果。改脚本里的输出路径变量时，要留意脚本内是否有"先删后建"逻辑会清掉目标目录。
- 误删后先别急着重跑：检查 `/tmp` 等临时目录是否还留有原始副本（trace 采集常把中间产物留在 tmp），可据此恢复。
